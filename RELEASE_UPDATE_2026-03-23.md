# Release Update — 2026-03-23

## What changed
- re-ran the automated exporter test suite (`45 passed, 10 skipped`)
- re-ran the live JSONPlaceholder demo pull
- refreshed `output/` and `sample_output/` proof artifacts from the new live run
- updated README and QA notes to reflect the latest validation pass

## Why this matters
This keeps the repo visibly maintained and shows that the template still works against a real public API instead of looking like a one-and-done portfolio drop.

## Suggested GitHub update text
Shipped a maintenance + proof-of-life update for the REST API to CSV/JSON template today:
- test suite re-verified
- live JSONPlaceholder run re-verified
- sample outputs refreshed
- docs updated with current QA status

Repo: https://github.com/FCDevelopments/rest-api-to-csv-json-template
